module.exports = {
    content: ["./src/**/*.{html,js}"],
    theme: {
      extend: {
        animation: {
          'pulse-slow': 'pulse 10s infinite',

        },
      },
    },
    plugins: [],
  }